# Plans Directory

`plans/` 保存已经确认设计后的实现计划。这里的文档不是想法列表，而是可直接执行的任务拆解。

## File Contract
- 一个计划文件只服务一个实现目标。
- 必须覆盖文件结构、任务顺序、测试方式、部署约束。
- 不允许使用 `TODO`、`TBD`、模糊占位或“类似前文”这类偷懒表达。

## Rules
- 计划必须和 `specs/` 中对应设计保持一致。
- 计划先锁边界，再拆任务，最后写验证。
- 若引入新目录或新架构边界，先更新对应目录的 `CLAUDE.md`。
