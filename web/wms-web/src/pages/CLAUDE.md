# pages

## 文件
- `DashboardPage.vue`: 工作台首页。
- `ReturnWorkbenchPage.vue`: 退货质检与处置工作页，负责页面布局与工作台状态机接线。
- `ReturnWorkbenchPage.test.ts`: 校验页面按钮和退货工作台状态机的接线没断。
- `SopAssistPage.vue`: SOP 辅助执行页。

## 原则
- 每页解决一个业务问题。
- 先展示决策信息，再展示辅助信息。
